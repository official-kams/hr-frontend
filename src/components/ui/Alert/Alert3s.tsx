// import React, {useEffect, useState} from 'react';
// import './Alert.scss';
// export default function Alert3s({text, visible, type}){
// 	const [isVisible, setIsVisible] = useState(visible);
//
// 	useEffect(() => {
// 		setIsVisible(visible);
// 	}, [visible]);
//
// 	return (
// 		<div className={`alert3s ${isVisible ? 'visible' : ''} ${type === 'ok' ? 'primary' : ''}`}>
// 			{text}
// 		</div>
// 	);
// };

import React from "react";

export default function Alert3s(){
  return(
    <>

    </>
  )
};